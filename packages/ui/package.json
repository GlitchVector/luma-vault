{
  "name": "@luma/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "//": "Consumed as source, like @luma/core. Tailwind cannot auto-detect classes outside the app's own tree, so apps/web/src/styles.css carries an explicit @source pointing here.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
