{
  "name": "@luma/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "//": "Consumed as source: no build step, no dist, nothing to go stale. Vite and tsc compile the TypeScript inline.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "unique-names-generator": "^4.7.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
